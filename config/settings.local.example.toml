# 复制为 config/settings.local.toml 后, 可写入本机专属的非机密覆盖项。
# 本文件最后加载, 会覆盖 settings.toml 和 settings.<env>.toml。

[paths]
# raw_dir = "/absolute/path/to/local/raw"
# processed_dir = "/absolute/path/to/local/processed"
# database_path = "/absolute/path/to/local/quant.duckdb"
# log_dir = "/absolute/path/to/local/log"
